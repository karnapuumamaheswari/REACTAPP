# React Router Example (reactapplication)

Instructions:

1. Install dependencies:

```bash
cd reactapplication
npm install
```

2. Start dev server:

```bash
npm run dev
```

Notes: run `npm install react-router-dom` if not installed. The app uses `index.html` -> `src/main.jsx`.
